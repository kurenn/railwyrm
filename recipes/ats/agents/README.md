# ATS Agents

Reference agent assets for ATS feature delivery live here.

Planned structure:

- `core/` shared ATS domain agent instructions
- `roles/` specialized agents (pipeline, reporting, scheduling)
- `checks/` reviewer agents for policy/test/regression checks
