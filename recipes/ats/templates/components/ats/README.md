# ATS Components

This folder contains ATS-specific view fragments copied into `app/components`.

The initial starter pack keeps behavior simple and relies primarily on
`untitled_ui` components rendered directly from views.
