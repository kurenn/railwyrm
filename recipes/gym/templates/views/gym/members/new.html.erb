<% content_for :title, "New Member" %>

<section class="space-y-6">
  <header>
    <h1 class="text-3xl font-semibold text-slate-900">New member</h1>
    <p class="mt-1 text-slate-500">Capture basic member details.</p>
  </header>

  <%= render "form", member: @member %>
</section>
